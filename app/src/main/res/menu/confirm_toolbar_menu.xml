<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_cart"
        android:icon="@drawable/ic_cart" android:title="Giỏ hàng"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/menu_chat"
        android:icon="@drawable/ic_chat" android:title="Chat"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/menu_notification"
        android:icon="@drawable/ic_notification" android:title="Thông báo"
        app:showAsAction="ifRoom" />
</menu>