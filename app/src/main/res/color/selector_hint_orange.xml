<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused -->
    <item android:color="@color/darkDay" android:state_focused="true"/>
    <!-- Disabled -->
    <item android:color="@color/darkDay" android:state_enabled="false"/>
    <!-- Default (enabled, not focused) -->
    <item android:color="@color/darkDay"/>
</selector>